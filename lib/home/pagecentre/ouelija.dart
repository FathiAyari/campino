import 'package:flutter/cupertino.dart';

class Ouelija extends StatelessWidget {
  const Ouelija({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
